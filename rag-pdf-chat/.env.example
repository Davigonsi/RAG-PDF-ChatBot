# OpenAI — used for embeddings (text-embedding-3-small) and answers (gpt-4o-mini)
OPENAI_API_KEY=

# Pinecone — create a dense serverless index (dim=1536, metric=cosine)
PINECONE_API_KEY=
PINECONE_INDEX_NAME=
PINECONE_ENVIRONMENT=
